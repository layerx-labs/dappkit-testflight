import { TruncateWalletPipe } from './truncate-wallet.pipe';

describe('TruncateWalletPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateWalletPipe();
    expect(pipe).toBeTruthy();
  });
});
